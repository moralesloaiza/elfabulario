---
import { SITE_TITLE } from '../consts';
import Buscador from './Buscador.astro';
import HeaderLink from './HeaderLink.astro';
---

<header>
	<div class="page header-inner">
		<a href="/" class="logo" aria-label={`${SITE_TITLE} · inicio`}>
			<span class="logo-text">{SITE_TITLE}</span>
		</a>
		<nav class="internal-links" aria-label="Navegación principal">
			<HeaderLink href="/">Inicio</HeaderLink>
			<HeaderLink href="/fabulas">Fábulas</HeaderLink>
			<HeaderLink href="/entradas">Bitácora</HeaderLink>
			<HeaderLink href="/correspondencia">Correspondencia</HeaderLink>
			<HeaderLink href="/sobre">Acerca de</HeaderLink>
			<Buscador />
		</nav>
	</div>
</header>

<style>
	header {
		background: var(--color-bg);
		border-bottom: var(--rule-hairline) solid var(--color-oro);
		position: relative;
	}
	/* Línea ornamental doble Art-Déco */
	header::after {
		content: "";
		position: absolute;
		left: 0;
		right: 0;
		bottom: -5px;
		height: var(--rule-hairline);
		background: var(--color-oro);
	}
	.header-inner {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: var(--space-md);
		padding-block: var(--space-md);
		flex-wrap: wrap;
	}
	.logo {
		text-decoration: none;
		color: var(--color-text);
	}
	.logo-text {
		font-family: var(--font-display);
		font-weight: var(--fw-bold);
		font-size: var(--fs-md);
		letter-spacing: var(--ls-widest);
		/* uppercase eliminado: Cinzel ya es all-caps */
		color: var(--color-text);
	}
	.logo:hover .logo-text,
	.logo:focus-visible .logo-text {
		color: var(--color-oro-texto);
	}
	.internal-links {
		display: flex;
		gap: var(--space-lg);
	}
	.internal-links :global(a) {
		font-family: var(--font-display);
		font-size: var(--fs-xs);
		font-weight: var(--fw-regular);
		letter-spacing: var(--ls-widest);
		/* uppercase eliminado: Cinzel ya es all-caps */
		color: var(--color-text);
		text-decoration: none;
		padding-block: var(--space-2xs);
		border-bottom: var(--rule-medium) solid transparent;
		transition: color var(--dur-base) var(--ease-standard),
		            border-color var(--dur-base) var(--ease-standard);
	}
	.internal-links :global(a:hover),
	.internal-links :global(a:focus-visible) {
		color: var(--color-oro-texto);
	}
	.internal-links :global(a.active) {
		font-weight: var(--fw-bold);
		border-bottom-color: var(--color-oro);
		text-decoration: none;
	}
	@media (max-width: 540px) {
		.header-inner {
			justify-content: center;
			text-align: center;
		}
		.internal-links {
			gap: var(--space-md);
		}
	}
</style>